version: '3.8'

services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_USER=support
      - POSTGRES_PASSWORD=1234
      #- POSTGRES_USER=postgres
      #- POSTGRES_PASSWORD=gbRRwSWdsmo5JNhR
    ports:
      - '5432:5436'
    volumes:
      - ~/docker/postgres-data:/var/lib/postgres